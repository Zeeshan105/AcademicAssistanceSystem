CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE QUIZ( QUIZNAME VARCHAR(50) NOT NULL, QUESTIONCONTENT VARCHAR(50) NOT NULL, CONSTRAINT CONSTRAINT1 PRIMARY KEY(QUIZNAME,QUESTIONCONTENT), CONSTRAINT CONSTRAINT2 FOREIGN KEY(QUESTIONCONTENT) REFERENCES QUESTION(QUESTIONCONTENT))
CREATE MEMORY TABLE QUESTION( QUESTIONCONTENT VARCHAR(50) NOT NULL PRIMARY KEY, OPTIONA VARCHAR(50), OPTIONB VARCHAR(50), OPTIONC VARCHAR(50), OPTIOND VARCHAR(50), ANSWER VARCHAR(50))
CREATE USER SA PASSWORD ""
GRANT DBA TO SA
SET WRITE_DELAY 20
SET SCHEMA PUBLIC
/ add insert statement here
